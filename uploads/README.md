This directory stores uploaded files in production.

When running with Docker Compose, this folder is bind-mounted to the backend container at `/app/uploads`.

Ensure this directory exists before starting containers.